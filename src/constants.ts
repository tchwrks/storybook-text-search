export const ADDON_ID = "storybook-text-search"; // Not sure if this is what you're supposed to put here
export const TOOL_ID = `${ADDON_ID}/tool`;
export const KEY = ADDON_ID;

export const EVENTS = {
  RESULT: `${ADDON_ID}/result`,
  REQUEST: `${ADDON_ID}/request`,
};
